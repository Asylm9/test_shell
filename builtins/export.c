#include "../minishell.h"

int	builtin_export(char **args, t_sh *shell)
{
	// export -> affiche env var par ordre ascii ainsi que var custom(valides ou non)

	//"export: not valid in this context:[arg]" code 1
}